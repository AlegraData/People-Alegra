-- WARNING: This schema is for context only and is not meant to be run.
-- Table order and constraints may not be valid for execution.

CREATE TABLE public.employee_bank (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  employee_id uuid NOT NULL,
  institucion_bancaria text,
  tipo_cuenta text,
  numero_cuenta text,
  clabe text,
  numero_cliente text,
  numero_cliente_interbancario text,
  correo_deel text,
  valido_desde timestamp with time zone NOT NULL DEFAULT now(),
  valido_hasta timestamp with time zone,
  es_actual boolean DEFAULT true,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT employee_bank_pkey PRIMARY KEY (id),
  CONSTRAINT employee_bank_employee_id_fkey FOREIGN KEY (employee_id) REFERENCES public.employees(id)
);
CREATE TABLE public.employee_education (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  employee_id uuid NOT NULL,
  nivel_educativo text,
  anio_formacion integer,
  institucion_educativa text,
  valido_desde timestamp with time zone NOT NULL DEFAULT now(),
  valido_hasta timestamp with time zone,
  es_actual boolean DEFAULT true,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT employee_education_pkey PRIMARY KEY (id),
  CONSTRAINT employee_education_employee_id_fkey FOREIGN KEY (employee_id) REFERENCES public.employees(id)
);
CREATE TABLE public.employee_events (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  employee_id uuid NOT NULL,
  event_type text NOT NULL,
  event_date date NOT NULL,
  role_name text,
  technical_team text,
  society text,
  contract_type text,
  compensation numeric,
  currency text DEFAULT 'USD'::text,
  valido_desde timestamp with time zone NOT NULL DEFAULT now(),
  valido_hasta timestamp with time zone,
  es_actual boolean DEFAULT true,
  created_at timestamp with time zone DEFAULT now(),
  source text,
  bonus numeric,
  previous_compensation numeric,
  reason_retirement text,
  reentry text,
  power_card numeric,
  wellness_card numeric,
  fuel_card numeric,
  area_id bigint,
  status text,
  country text,
  metadata jsonb,
  tmd text,
  employee_type text,
  CONSTRAINT employee_events_pkey PRIMARY KEY (id),
  CONSTRAINT employee_events_employee_id_fkey FOREIGN KEY (employee_id) REFERENCES public.employees(id)
);
CREATE TABLE public.employee_pep (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  employee_id uuid NOT NULL,
  pep text,
  detalle_pep text,
  familiares_pep text,
  actividades_pep text,
  valido_desde timestamp with time zone NOT NULL DEFAULT now(),
  valido_hasta timestamp with time zone,
  es_actual boolean DEFAULT true,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT employee_pep_pkey PRIMARY KEY (id),
  CONSTRAINT employee_pep_employee_id_fkey FOREIGN KEY (employee_id) REFERENCES public.employees(id)
);
CREATE TABLE public.employee_personal_info (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  employee_id uuid NOT NULL,
  nombres text,
  primer_apellido text,
  segundo_apellido text,
  numero_documento text,
  tipo_documento text,
  fecha_nacimiento date,
  genero text,
  estado_civil text,
  nacionalidad text,
  email_personal text,
  telefono text,
  pais_residencia text,
  departamento text,
  ciudad text,
  direccion text,
  codigo_postal text,
  tipo_sangre text,
  enfermedades text,
  alergias text,
  medicamentos text,
  contacto_emergencia text,
  parentesco text,
  telefono_emergencia text,
  estado_familiar text,
  num_hijos integer,
  nombres_hijos text,
  valido_desde timestamp with time zone NOT NULL DEFAULT now(),
  valido_hasta timestamp with time zone,
  es_actual boolean DEFAULT true,
  created_at timestamp with time zone DEFAULT now(),
  metadata jsonb DEFAULT '[]'::jsonb,
  barrio text,
  codigo_pais_telefono text,
  codigo_pais_emergencia text,
  fechas_nacimiento_hijos text,
  zona text,
  estrato text,
  numero_personas_a_cargo text,
  nivel_educativo text,
  categoria_educativa text,
  updated_at timestamp with time zone DEFAULT now(),
  direccion_componentes jsonb DEFAULT '{}'::jsonb,
  CONSTRAINT employee_personal_info_pkey PRIMARY KEY (id),
  CONSTRAINT employee_personal_info_employee_id_fkey FOREIGN KEY (employee_id) REFERENCES public.employees(id)
);
CREATE TABLE public.employee_ss (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  employee_id uuid NOT NULL,
  eps_afiliado text,
  eps text,
  zona text,
  estrato text,
  pensiones_afiliado text,
  fondo_pensiones text,
  cesantias_afiliado text,
  fondo_cesantias text,
  curp text,
  rfc text,
  cp_fiscal text,
  imss text,
  beneficiario text,
  descuentos text,
  seguridad_social_españa text,
  afp text,
  tipo_afp text,
  cuspp text,
  hijos_menores text,
  valido_desde timestamp with time zone NOT NULL DEFAULT now(),
  valido_hasta timestamp with time zone,
  es_actual boolean DEFAULT true,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT employee_ss_pkey PRIMARY KEY (id),
  CONSTRAINT employee_ss_employee_id_fkey FOREIGN KEY (employee_id) REFERENCES public.employees(id)
);
CREATE TABLE public.employee_technical (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  employee_id uuid NOT NULL,
  modelo_computador text,
  valido_desde timestamp with time zone NOT NULL DEFAULT now(),
  valido_hasta timestamp with time zone,
  es_actual boolean DEFAULT true,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT employee_technical_pkey PRIMARY KEY (id),
  CONSTRAINT employee_technical_employee_id_fkey FOREIGN KEY (employee_id) REFERENCES public.employees(id)
);
CREATE TABLE public.employees (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  gid bigint NOT NULL UNIQUE,
  email text NOT NULL UNIQUE,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT employees_pkey PRIMARY KEY (id)
);
CREATE TABLE public.finance_form_versions (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  employee_id uuid NOT NULL,
  version_number integer NOT NULL DEFAULT 1,
  form_data jsonb NOT NULL,
  status text DEFAULT 'Completado'::text,
  created_at timestamp with time zone DEFAULT now(),
  created_by text,
  is_current boolean DEFAULT true,
  notes text,
  CONSTRAINT finance_form_versions_pkey PRIMARY KEY (id),
  CONSTRAINT finance_form_versions_employee_id_fkey FOREIGN KEY (employee_id) REFERENCES public.employees(id)
);
CREATE TABLE public.onboarding_form_submissions (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  employee_id uuid,
  email_ingreso text NOT NULL,
  numero_documento text,
  form_data jsonb NOT NULL,
  file_urls jsonb DEFAULT '{}'::jsonb,
  submitted_at timestamp with time zone DEFAULT now(),
  is_current boolean DEFAULT true,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT onboarding_form_submissions_pkey PRIMARY KEY (id),
  CONSTRAINT onboarding_form_submissions_employee_id_fkey FOREIGN KEY (employee_id) REFERENCES public.employees(id)
);
CREATE TABLE public.talent_form_versions (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  employee_id uuid NOT NULL,
  version_number integer NOT NULL DEFAULT 1,
  form_data jsonb NOT NULL,
  status text DEFAULT 'Completado'::text,
  created_at timestamp with time zone DEFAULT now(),
  created_by text,
  is_current boolean DEFAULT true,
  notes text,
  CONSTRAINT talent_form_versions_pkey PRIMARY KEY (id),
  CONSTRAINT talent_form_versions_employee_id_fkey FOREIGN KEY (employee_id) REFERENCES public.employees(id)
);
CREATE TABLE public.user_roles (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL UNIQUE,
  email text NOT NULL UNIQUE,
  role text NOT NULL CHECK (role = ANY (ARRAY['admin'::text, 'manager'::text, 'viewer'::text])),
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT user_roles_pkey PRIMARY KEY (id),
  CONSTRAINT user_roles_user_id_fkey FOREIGN KEY (user_id) REFERENCES auth.users(id)
);